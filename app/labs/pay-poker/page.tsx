import PayPoker from '@/components/labs/PayPoker';

export const metadata = {
  title: 'Pay Poker - DAMA Labs',
  description: 'Gerenciador de torneio de poker com cálculo de pagamentos',
};

export default function PayPokerPage() {
  return <PayPoker />;
}